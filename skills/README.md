# AI Skills

This directory contains reusable skills and workflows for AI agents.

## Format
Store skills as Markdown files.
Example: `create_component.md` - Steps to create a new React component with the correct boilerplate.
